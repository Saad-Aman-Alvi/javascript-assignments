    // Declare and assign email
        let email = "example@example.com";

        // Create message using string concatenation
        let message = "My email address is " + email;

        // Show the message in an alert box
        alert(message);